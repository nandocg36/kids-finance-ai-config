{
  "_version": "1.1.1",
  "_description": "Índice principal de configuração IA - Kids Finance App",
  "_lastUpdated": "2025-07-23",

  "config_files": {
    "safety_rules": "safety/safety_rules.json",
    "crisis_patterns": "safety/crisis_patterns.json",
    "personality_rules": "personality/personality_rules.json",
    "system_prompt_template": "personality/system_prompt_template.json",
    "finance_rules": "finance/finance_rules.json",
    "greetings": "greetings/greetings.json",
    "suggested_tasks": "tasks/suggested_tasks.json",
    "screen_time_rules": "screen_time/screen_time_rules.json",
    "email_recommendations": "email/email_recommendations.json",
    "onboarding_patterns": "onboarding/onboarding_patterns.json"
  },

  "notes": [
    "Todos os arquivos são carregados via HTTP a partir do repositório GitHub",
    "Cache local é usado para fallback quando não há internet",
    "Para atualizar regras, edite os arquivos JSON no GitHub e o app atualiza automaticamente",
    "NUNCA edite regras diretamente no código Dart - sempre use os JSONs",
    "NUNCA use letras repetidas (Eee, Uau, etc.) nos textos - o TTS lê errado",
    "Use acentuação correta nos textos lidos pelo TTS (você, não, criança, até, etc.)",
    "OBRIGATÓRIO no app: substituir {displayName} pelo nome da criança e {aiName} pelo nome que ela deu à IA em TODO texto injetado no prompt (incl. personality_rules, system_prompt, safety, cumprimentos). Se não substituir, a IA pode falar 'display name'. Usar o mesmo valor que em greetings {nome} para {displayName}. Padrão de chaves: uma chave {displayName}, não {{displayName}} (exceto finance/screen_time que usam {{ }} conforme o motor do app)."
  ]
}
